node_modules
dist
*.log
.DS_Store
_to_delete

# Internal working notes and design-QA artefacts. The Pages repo has to be
# public for free hosting, so anything in it is world-readable — keep the
# working material out and let it stay in Drive.
AGENTS.md
EDITABLE-HANDOVER.md
design-qa.md
archive-superseded/
*.jpg
# ...but the landing page ships real .jpg assets. The rule above is for the
# QA screenshots in the repo root, not for anything served to users.
!public/assets/landing/**/*.jpg
approved-design-exact.png
source-design.png
